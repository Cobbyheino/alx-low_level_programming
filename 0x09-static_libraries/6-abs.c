#include "main.h"

/**
 * _abs - finds the absolute value of an int
 * @n: int to be directly checked
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
